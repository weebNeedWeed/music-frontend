import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  title: {
    color: "#0F1E36",
    fontWeight: "bold",
    fontSize: "20px",
  },
});

export default useStyles;
